import React from "react";
import { Chip } from "@mui/material";

const AddTags = () => {
  return (
    <div className='flex  gap-3 justify-between'>
      <Chip label='Add Tags' onClick={() => {}} onDelete={() => {}} />
      <Chip label='Add Tags' onClick={() => {}} onDelete={() => {}} />
    </div>
  );
};

export default AddTags;
